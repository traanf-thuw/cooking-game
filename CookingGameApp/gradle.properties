# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# JVM args for Gradle daemon: give it more memory
org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8

# Kotlin daemon memory limit: ensure Kotlin compiler gets enough
kotlin.daemon.jvm.options=-Xmx1024m

# AndroidX package structure
android.useAndroidX=true

# Kotlin code style for this project
kotlin.code.style=official

# Namespacing R class to reduce R class size
android.nonTransitiveRClass=true

# Optional: enable Gradle parallel build for performance (safe if your modules are decoupled)
# org.gradle.parallel=true
