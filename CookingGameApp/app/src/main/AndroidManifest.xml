<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.cookinggameapp">


    <uses-permission android:name="android.permission.BLUETOOTH"
        tools:ignore="CoarseFineLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CookingGameApp"
        tools:targetApi="31">

        <!-- Optional for Nearby in older devices -->
        <uses-library android:name="org.apache.http.legacy" android:required="false"/>

        <!-- All Activities -->
        <activity android:name=".InstructionActivity" android:exported="false" />
        <activity android:name=".JoinActivity" android:exported="false" />
        <activity android:name=".CreateRoomActivity" android:exported="false" />

        <!-- TEMPORARY: Make PlayGameActivity the launcher -->
        <activity
            android:name=".MenuActivity"
            android:theme="@style/Theme.CookingGameApp"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".InstructionActivity" android:exported="false" />
        <activity android:name=".JoinRoomActivity" android:exported="false" />
        <activity android:name=".CreateRoomActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />
        <activity android:name=".WaitingActivity" android:exported="false" />

    </application>

</manifest>
